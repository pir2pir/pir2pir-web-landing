# Production env for pir2pir-web-landing — DO NOT COMMIT.
# Written to /opt/pir2pir-web-landing/.env by the deploy workflow from the ENV_PROD variable.
# The page is static, so nothing here is secret; the file keeps the port configurable and matches
# the deploy shape of the other services.

LANDING_HTTP_PORT=8082
