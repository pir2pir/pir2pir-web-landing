{
  "name": "pir2pir-web-landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Landing page for pir2pir.ru",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && node scripts/build.mjs",
    "test": "node scripts/boot.test.mjs",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit"
  },
  "//": "Nothing here ships to the browser: React renders the pages at build time and the site is served as static HTML.",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "esbuild": "^0.25.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=20.0"
  }
}
